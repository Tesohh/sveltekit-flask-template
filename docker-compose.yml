services:
  backend:
    build: ./backend
    ports:
      - 5001:5000
    environment:
        NODE_ENV: development
    volumes:
      - ./backend:/app

  frontend:
    build: ./frontend
    ports:
      - 5173:5173
    volumes:
      - ./frontend:/app
      - /app/node_modules
